# AWS ec2 접속방법
## AWS 사이트 ec2 인스턴스에서
``"인스턴트"`` 클릭 오른쪽 위 ``"연결"``

## 로컬 터미널에서 aws 접속 방법
ssh -i /path/to/key.pem ubuntu@EC2-IP  
ssh -i [키 경로] [서버에 따라 달라짐 gpt에 물어봐]@[EC2-IP]  
예시)  ssh -i ~/Desktop/폴더명/파일명.pem ubuntu@0.0.0.0  

